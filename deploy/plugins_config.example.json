{
  "_comment": [
    "Copy to <SQREADER_HOME>/plugins_config.json and pass it with",
    "--plugins-config. Only keys you want to override need to appear under",
    "'config' — everything else falls back to DEFAULT_CONFIG in the plugin.",
    "",
    "Alerts are written to --stats-db and read back from GET /api/alerts,",
    "which is live-gated (an alert names a player and calls them a cheater).",
    "",
    "Durations are GAME-CLOCK SECONDS and mean the same thing at any --hz;",
    "the old 'sustained ticks' knobs (and their re-scale-by-hand warning)",
    "are gone. See sqreader/plugins/cheat_detect.py for every tunable and",
    "the reasoning each threshold carries."
  ],

  "cheat_detect": {
    "enabled": true,
    "config": {
      "report_cooldown_minutes": 5.0,

      "detect_speedhack": true,
      "speed_max_foot_mps": 18.0,
      "speed_sustained_seconds": 16.0,

      "detect_infinite_ammo": true,
      "inf_ammo_min_damage_events": 3,
      "inf_ammo_min_window_seconds": 30.0,
      "inf_ammo_stale_seconds": 15.0,

      "detect_remote_melee": true,
      "remote_melee_max_dist_m": 25.0,

      "detect_magic_bullet": false
    }
  }
}
